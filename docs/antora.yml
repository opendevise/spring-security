name: ROOT
version: true
ext:
  collector:
    run:
      command: gradlew --no-scan :spring-security-docs:generateAntora
      #command: gradlew --console rich --no-scan :spring-security-docs:generateAntora
      local: true
    scan:
      dir: ./build/generateAntora
